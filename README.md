### Tea Lovers

Для запуску можна скористатись командою:

- ```nest start```

Для збірки виконати команди:

- ```npm run build```
- ```node dist/main.js```

Щоб запустити docker:

- ```docker-compose up -d```

